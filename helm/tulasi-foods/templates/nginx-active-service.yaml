{{- if eq .Values.color "blue" }}
apiVersion: v1
kind: Service
metadata:
  name: nginx-active
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: nginx
    version: {{ .Values.activeColor }}
  ports:
    - port: 80
      targetPort: 80
{{- end }}
